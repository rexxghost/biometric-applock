# Add project specific ProGuard rules here.
# This project ships with minifyEnabled false by default, so these rules
# are provided for completeness if you turn minification on later.

-keep class com.example.applocker.** { *; }
